#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int main(){
	double a = 3.4;
	// * (double) a : - делает число вещественным
	// * trunс(a) : - отрезает дробную часть
	// * floor(a) : - округляет вниз
	// * ceil(a) : - округляет вверх
	// * round(a) : - округляет стандартно
	// * Масса Земли равна 5.972*10^24 килограмм. 
	// 	Здесь 5.972 — мантисса, а 24 — экспонента.
	// * cout << setprecision(3) << fixed; - вывести число 
	//	с точностью 3 знака после запятой
}
